# 10连抽实现方案

## 整体思路

将循环调用10次单抽改为分阶段批量处理：账户扣减合并为1次事务、10次抽奖计算并行执行、库存扣减与抽奖次数更新通过Lua脚本保证原子性、中奖记录批量写入。总耗时从约500ms压缩到约50ms以内。

## 关于幂等与加锁的说明

### 幂等

幂等控制由前端负责（如按钮防重提交、客户端生成唯一请求ID），服务端不再做幂等校验。

### 为什么原来要加锁

原来加用户维度分布式锁的目的是：防止同一用户同时发起两次10连抽，导致两次请求读到相同的Redis抽奖次数，计算出重叠的虚拟次数，进而导致次数解锁规则重复触发。

### 不加锁的替代方案

本方案不在服务端加分布式锁，而是通过**Redis INCRBY先递增再计算**的方式规避并发问题：

- 10连抽开始时，先用INCRBY原子地将抽奖次数加10，拿到递增后的值
- 从递增后的值倒推本次10连抽的虚拟次数区间
- 两个并发请求会拿到不同的递增值，虚拟次数区间不会重叠

代价是：如果后续流程失败（如库存不足、写库失败），需要通过INCRBY -10回滚抽奖次数，走补偿机制。

---

## 详细流程

### 第一步：账户扣减（一个DB事务）

通过路由中间件绑定用户对应的分库，在同一个数据库连接、同一个事务内完成以下操作：

- 总账户扣减10次剩余抽奖次数
- 月账户扣减10次剩余次数
- 日账户扣减10次剩余次数
- 写入一条10连抽的抽奖订单记录

事务保证以上操作要么全部成功，要么全部回滚。

注意：累计抽奖次数不再在此步更新，而是后续通过Redis原子操作维护。

### 第二步：Redis抽奖次数递增 & 计算虚拟次数

通过Redis INCRBY原子地将用户累计抽奖次数加10，拿到递增后的值。假设递增前次数为N，则本次10连抽的虚拟次数区间为 [N+1, N+10]。

虚拟次数是一个内存临时变量，不写入数据库，作用是给每个并行的抽奖任务提供"你是第几抽"的参照，用于次数解锁等需要累计次数判断的规则。这样并行任务不需要各自去查Redis，避免了并发读取的可见性问题。

如果后续流程失败，通过INCRBY -10回滚本次递增。

### 第三步：并行抽奖计算

用线程池并行执行10次抽奖，每次抽奖携带各自的虚拟次数，走完整的规则链和决策树流程，进行概率计算。

所有10次均为正常随机抽奖，不在此时做保底判断。

10次抽奖全部完成后，收集结果。

### 第四步：保底判断

10次抽奖结果收集完毕后，遍历虚拟次数区间，检查是否有某次抽奖的虚拟次数命中保底线（如累计100抽必中实物奖）。

- 如果命中保底线，将对应位置的中奖结果替换为保底奖品ID
- 保底奖品也要正常扣减库存，如果库存不足则走兜底奖逻辑
- 未命中保底线则保持原有随机抽奖结果不变

这种"先抽后判断"的方式保证了保底逻辑与随机抽奖流程解耦，抽奖计算完全并行不受保底影响。

### 第五步：批量库存扣减 & 抽奖次数确认（Lua脚本保证原子性）

将10次抽奖的库存扣减和抽奖次数确认合并为一个Lua脚本执行，保证原子性。

Lua脚本逻辑：
1. 遍历10个中奖结果，逐个检查并扣减对应奖品的Redis库存（decr）
2. 如果某个奖品库存不足，标记该位置走兜底奖
3. 抽奖次数已在第二步INCRBY递增，此处无需再次更新
4. 所有库存扣减要么全部成功，要么脚本返回失败信息

通过Lua脚本保证：库存扣减是一个原子操作，不会出现部分扣减成功部分失败的情况。如果脚本执行失败，需要重新执行或走补偿流程。

同时将扣减消息写入Redis延迟队列，用于后续异步更新数据库库存。

### 第六步：批量写入中奖记录（一个DB事务）

再次绑定路由，在同一个事务内完成：

- 10条中奖记录批量写入数据库（一条SQL完成）
- 写入本地消息表，记录待发送的发奖MQ消息

中奖记录和本地消息表在同一个事务中，保证要么都成功，要么都回滚。

### 第七步：异步发奖

MQ消息不直接在业务事务中发送，而是通过定时任务扫描本地消息表，将待发送的消息投递到MQ，发送成功后更新消息状态。如果发送失败，定时任务会重试，保证消息最终送达。

---

## 崩溃恢复机制

| 崩溃时机 | 状态 | 恢复方式 |
|---|---|---|
| 第一步之后、第二步之前 | 账户已扣减，Redis次数未递增 | 定时任务扫描"订单已创建但无中奖记录"的数据，重新执行后续流程 |
| 第二步之后、第五步之前 | 账户已扣减，Redis次数已递增，库存未扣减 | INCRBY -10回滚次数，定时任务扫描异常订单重新执行或回滚账户 |
| 第五步之后、第六步之前 | 账户已扣减，次数已递增，库存已扣减，记录未写入 | 定时任务扫描异常订单，重新写入中奖记录 |
| 第六步之后 | 全部完成 | 无需处理 |

核心原则：定时任务定期扫描"订单已创建但无对应中奖记录"的10连抽数据，发现异常数据后做补偿，保证用户权益不丢失。

---

## 性能优化效果对比

| 环节 | 循环10次单抽 | 优化后10连抽 |
|---|---|---|
| 账户扣减 | 10次DB事务 | 1次DB事务 |
| 抽奖计算 | 串行执行，约100ms | 并行执行，约15ms |
| 库存扣减 | 10次Redis网络往返 | 1次Lua脚本 |
| 中奖记录写入 | 10次DB插入 | 1次批量插入 |
| MQ消息发送 | 10次消息发送 | 1次批量投递 |
| 整体耗时 | 约500ms | 约50ms |

---

## 一致性保障

| 风险场景 | 保障手段 |
|---|---|
| 并发两次10连抽 | Redis INCRBY原子递增，虚拟次数区间不重叠 |
| 写入中奖记录失败 | 同事务回滚，账户扣减一并回滚 |
| 库存扣减与次数更新不一致 | Lua脚本保证原子性，要么全部成功要么全部失败 |
| 进程中途崩溃 | 定时任务扫描异常数据做补偿，INCRBY回滚抽奖次数 |
| MQ消息发送失败 | 本地消息表 + 定时任务重投递 |
| 库存不足 | Lua脚本内检测，走兜底奖逻辑 |
